// DB stub — 나중에 실제 DB 연결로 교체 (PostgreSQL, SQLite 등)
const db = {
  async saveAnalysis(data) {
    // TODO: implement DB storage
    // await pool.query('INSERT INTO analyses ...', [data])
  }
}

module.exports = db
